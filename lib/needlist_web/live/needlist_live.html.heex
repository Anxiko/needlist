<div>
  <h1 class="dark:text-white pb-10 text-4xl">
    Needlist for <%= @username %>
  </h1>

  <%= if @current_page != nil do %>
    <div class="relative overflow-x-auto">
      <table class="rtl:text-right dark:text-gray-400 w-full text-sm text-left text-gray-500 table-fixed">
        <thead class="bg-gray-50 dark:bg-gray-700 dark:text-gray-400 text-xs text-gray-700 uppercase">
          <tr>
            <.table_header state={@state} column_key={:artist} column_name="Artist" />
            <.table_header state={@state} column_key={:title} column_name="Title" />
            <.table_header state={@state} column_key={:label} column_name="Label" />
            <.table_header state={@state} column_name="Formats" />
            <.table_header state={@state} column_key={:year} column_name="Year" />
            <.table_header state={@state} column_key={:min_price} column_name="Min price" />
            <.table_header state={@state} column_key={:avg_price} column_name="Avg price" />
            <.table_header state={@state} column_key={:max_price} column_name="Max price" />
          </tr>
        </thead>
        <tbody>
          <%= for item <- elem(@current_page, 1).items do %>
            <tr class="dark:bg-gray-800 dark:border-gray-700 bg-white border-b">
              <td scope="row" class=" px-6 py-4">
                <.want_artists artists={item.basic_information.artists} />
              </td>
              <td class="px-6 py-4">
                <.want_title want={item} />
              </td>
              <td class="px-6 py-4">
                <.want_labels labels={item.basic_information.labels} />
              </td>
              <td class="px-6 py-4">
                <.want_formats formats={item.basic_information.formats} />
              </td>
              <td class="px-6 py-4">
                <.want_year year={item.basic_information.year} />
              </td>
              <td class="px-6 py-4">
                <.want_price price={item.min_price} />
              </td>
              <td class="px-6 py-4">
                <.want_price price={item.avg_price} />
              </td>
              <td class="px-6 py-4">
                <.want_price price={item.max_price} />
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="justify-evenly flex items-end mt-4">
      <div>
        <.table_pagination username={@username} current_page={elem(@current_page, 1)} state={@state} />
      </div>
      <div>
        <.per_page_selector per_page_options={[25, 50, 100, 250]} selected_per_page={@state.per_page} />
      </div>
    </div>
  <% end %>
</div>
